#include <stdio.h>

/**
 * main - lower and upper case function
 *
 * Return: upper and lower case alphabets
 */

int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		putchar(i);
	for (i = 'A' ; i <= 'Z' ; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
